from typing import Iterable


def normalize_records(records: Iterable[dict]) -> list[dict]:
    """TODO: Clean and normalize raw RSS items."""
    raise NotImplementedError('Pipeline transform is not wired yet.')
